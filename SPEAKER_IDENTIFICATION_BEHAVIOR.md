# 🔊 Поведение при enable_speaker_identification: False

## 🎯 Что происходит?

Когда `enable_speaker_identification: False`:

1. ✅ **Спикеры НЕ определяются** (пропускается AI определение)
2. ✅ **Транскрипт СОХРАНЯЕТСЯ** (без имен спикеров)
3. ✅ **Публикуется на Google Sheets** (новая строка создается)
4. ✅ **Загружается на Google Drive** (как Google Doc)

## 📊 Процесс обработки

### Этап 1: Загрузка и транскрибция

```
Видео загружается на сервер
    ↓
Сервер обрабатывает видео
    ↓
Транскрипт готов
```

### Этап 2: Определение спикеров (если enable_speaker_identification: true)

```
enable_speaker_identification: true
    ↓
Приложение пытается определить спикеров
    ↓
Если спикеры определены:
    └─ Обновляет спикеров на сервере
    └─ Предлагает ручное редактирование в Telegram
    └─ Сохраняет с именами спикеров
```

### Этап 2: Пропуск определения спикеров (если enable_speaker_identification: false)

```
enable_speaker_identification: false
    ↓
Приложение ПРОПУСКАЕТ определение спикеров
    ↓
Сохраняет транскрипт БЕЗ имен спикеров
    └─ Спикеры остаются как "Speaker 1", "Speaker 2", и т.д.
```

### Этап 3: Сохранение транскрипта

```
Транскрипт сохраняется локально
    ├─ Имена файлов: Meeting_Name_transcript.txt
    ├─ Содержимое: Текст с спикерами (или без)
    └─ Кодировка: UTF-8
```

### Этап 4: Публикация на Google Sheets

```
Создается новая строка в Meeting_Logs:
    ├─ Meeting Name: "Front - Aorta weekly sync"
    ├─ Meeting Time: "2026-01-27 14:30:00"
    ├─ Meeting Type: "General" (или определено AI)
    ├─ Speakers: "" (пусто, если enable_speaker_identification: false)
    ├─ Summary: "Краткое описание встречи"
    ├─ Key Discussion Topics: "Список тем"
    ├─ Transcript Link: "https://docs.google.com/document/..."
    └─ Status: "Completed"
```

### Этап 5: Загрузка на Google Drive

```
Транскрипт загружается как Google Doc
    ├─ Имя: "Meeting_Name_transcript"
    ├─ Папка: drive_transcript_folder_id
    └─ Содержимое: Полный текст транскрипта
```

## 📋 Сравнение: enable_speaker_identification: true vs false

| Функция | true | false |
|---------|------|-------|
| Определение спикеров | ✅ Да | ❌ Нет |
| Сообщение в Telegram | ✅ Да | ❌ Нет |
| Ручное редактирование | ✅ Да | ❌ Нет |
| Сохранение транскрипта | ✅ С именами | ✅ Без имен |
| Публикация на Sheets | ✅ Да | ✅ Да |
| Загрузка на Drive | ✅ Да | ✅ Да |
| Создание строки в Sheets | ✅ Да | ✅ Да |

## 🔄 Код: Как это работает?

### 1. Пропуск определения спикеров

```python
# video_uploader.py, строка 278-283
enable_speaker_identification = self.config.get("enable_speaker_identification", True)
if enable_speaker_identification:
    logger.info(f"Starting speaker identification for job {job_id}")
    identified_speakers = self._identify_speakers(transcript_data, job_id)
else:
    logger.info(f"Speaker identification disabled for job {job_id}")
    identified_speakers = None
```

### 2. Пропуск предложения ручного назначения

```python
# video_uploader.py, строка 314-320
if enable_speaker_identification:
    speakers = self._extract_speakers(transcript_data)
    if speakers:
        self._offer_manual_speaker_assignment(job_id, speakers, transcript_data, ...)
```

### 3. Сохранение транскрипта

```python
# video_uploader.py, строка 341
transcript_path = self._save_transcript_file(save_path, transcript_data, job_id=job_id)
```

**Важно:** Транскрипт сохраняется ВСЕГДА, независимо от `enable_speaker_identification`!

### 4. Публикация на Google Sheets

```python
# main.py, строка 1179-1190
speakers = self.uploader._extract_speakers(transcript_data)

# Final speakers list for the spreadsheet cell
final_speakers = []
for s in speakers:
    if s in mapping:
        final_speakers.append(mapping[s])
    else:
        final_speakers.append(s)

speakers_str = ", ".join(sorted(list(set(final_speakers)))) if final_speakers else ""
```

**Важно:** Если спикеры не определены, `speakers_str` будет пустой строкой, но строка все равно создается!

## 💡 Примеры

### Пример 1: enable_speaker_identification: true

**Конфигурация:**
```json
{
    "enable_speaker_identification": true,
    "openrouter_api_key": "sk-or-v1-..."
}
```

**Результат в Google Sheets:**
```
Meeting Name: Front - Aorta weekly sync
Speakers: John Smith, Sarah Johnson, Mike Davis
Summary: Обсуждались вопросы...
```

**Результат в транскрипте:**
```
John Smith: Привет всем, начнем встречу...
Sarah Johnson: Согласна, давайте обсудим...
Mike Davis: Я предлагаю...
```

### Пример 2: enable_speaker_identification: false

**Конфигурация:**
```json
{
    "enable_speaker_identification": false
}
```

**Результат в Google Sheets:**
```
Meeting Name: Front - Aorta weekly sync
Speakers: (пусто)
Summary: Обсуждались вопросы...
```

**Результат в транскрипте:**
```
Speaker 1: Привет всем, начнем встречу...
Speaker 2: Согласна, давайте обсудим...
Speaker 3: Я предлагаю...
```

## ✅ Проверка: Что происходит?

### Когда enable_speaker_identification: false

1. ✅ **Видео загружается** - файл перемещается в Transcribed
2. ✅ **Сервер обрабатывает** - получается транскрипт
3. ❌ **Спикеры НЕ определяются** - пропускается AI определение
4. ❌ **Нет сообщения в Telegram** - о спикерах не сообщается
5. ✅ **Транскрипт сохраняется** - локально как .txt файл
6. ✅ **Публикуется на Sheets** - новая строка создается
7. ✅ **Загружается на Drive** - как Google Doc
8. ✅ **Результаты готовы** - без имен спикеров

## 📚 Связанная документация

- `SPEAKER_IDENTIFICATION_TOGGLE.md` - Описание параметра
- `SPEAKER_IDENTIFICATION_FIX.txt` - Исправление логики
- `PARAMETERS_EXPLAINED.md` - Объяснение параметров

---

**Дата:** 27 января 2026  
**Статус:** ✅ Поведение правильное  
**Версия:** 2.1
