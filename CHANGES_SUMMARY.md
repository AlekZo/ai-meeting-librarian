# 📝 Резюме изменений

## 🎯 Основное улучшение

Приложение теперь работает в **двухэтапном режиме** вместо одноэтапного:

### ❌ Старая архитектура (одноэтапная)
```
watch_folder → Переименование → Копирование → Загрузка → output_folder
(все автоматически)
```

### ✅ Новая архитектура (двухэтапная)
```
watch_folder → Переименование → (файл остается)
                                    ↓
                            (пользователь вручную переносит)
                                    ↓
                        to_transcribe_folder → Загрузка → output_folder
```

---

## 📋 Список изменений

### 1. Конфигурация (`config/config.py` и `config.json.example`)

**Добавлены новые параметры:**
- `to_transcribe_folder` - папка для видео, готовых к транскрипции
- `output_folder` - папка для архива обработанных видео

**Обновлена валидация:**
- Проверка существования `to_transcribe_folder`
- Проверка существования `output_folder`

### 2. Инициализация (`main.py`)

**Добавлены:**
- `self.transcribe_monitor` - второй монитор для `to_transcribe_folder`
- Инициализация второго монитора в методе `initialize()`
- Запуск второго монитора в методе `run()`
- Остановка второго монитора в методе `shutdown()`

### 3. Обработка видео (`main.py`)

**Изменены методы:**

#### `on_video_created()` (переименование)
- Добавлен префикс `[WATCH_FOLDER]` в логи
- Файл остается в `watch_folder` после переименования
- Отправляет уведомление пользователю

#### `handle_successful_rename()` (новая логика)
- **Старое поведение:** копирование в output_folder, загрузка, удаление
- **Новое поведение:** просто уведомление, файл остается

**Добавлены методы:**

#### `on_video_for_transcription()` (новый метод)
- Обработка видео из `to_transcribe_folder`
- Извлечение информации из имени файла
- Загрузка на сервер транскрипции
- Добавлен префикс `[TO_TRANSCRIBE_FOLDER]` в логи

### 4. Загрузка видео (`video_uploader.py`)

**Добавлены методы:**

#### `_move_file_to_output()` (новый метод)
- Перемещает файл из `to_transcribe_folder` в `output_folder`
- Вызывается после успешной загрузки на сервер
- Использует `os.rename()` для перемещения

**Изменены методы:**

#### `_process_upload()` (обновлена логика)
- Добавлен вызов `_move_file_to_output()` после успешной загрузки
- Файл перемещается в `output_folder` перед запуском транскрипции

---

## 📁 Структура папок

### Старая архитектура
```
watch_folder/
├─ Video_2026-01-26_20-30-03.mp4
└─ Front - Aorta weekly sync_2026-01-26_20-30-03.mp4 (переименованный)
   (затем копируется в output_folder и удаляется)

output_folder/
└─ Front - Aorta weekly sync_2026-01-26_20-30-03.mp4 (архив)
```

### Новая архитектура
```
watch_folder/
├─ Video_2026-01-26_20-30-03.mp4
└─ Front - Aorta weekly sync_2026-01-26_20-30-03.mp4 (переименованный, остается)

to_transcribe_folder/
└─ Front - Aorta weekly sync_2026-01-26_20-30-03.mp4 (пользователь переносит)
   (затем загружается и удаляется)

output_folder/
└─ Front - Aorta weekly sync_2026-01-26_20-30-03.mp4 (архив)
```

---

## 🔄 Процесс обработки видео

### Старая архитектура
```
1. Видео появляется в watch_folder
2. Переименование по встречам
3. Копирование в output_folder
4. Загрузка на сервер
5. Удаление из watch_folder
6. Обработка транскрипции
```

### Новая архитектура
```
1. Видео появляется в watch_folder
2. Переименование по встречам
3. Файл остается в watch_folder
4. Пользователь вручную переносит в to_transcribe_folder
5. Загрузка на сервер
6. Перемещение в output_folder
7. Обработка транскрипции
```

---

## ✨ Преимущества новой архитектуры

| Аспект | Старая | Новая |
|--------|--------|-------|
| **Контроль** | Автоматический | Пользователь решает |
| **Гибкость** | Все файлы обрабатываются | Можно выбрать какие файлы |
| **Безопасность** | Файлы удаляются | Файлы архивируются |
| **Параллелизм** | Один процесс | Два независимых процесса |
| **Отладка** | Сложнее | Проще (разделение ответственности) |

---

## 🔧 Технические детали

### Два независимых монитора

```python
# Монитор 1: watch_folder (переименование)
self.monitor = FileMonitor(
    watch_folder,
    video_extensions,
    self.on_video_created  # Переименование
)

# Монитор 2: to_transcribe_folder (транскрипция)
self.transcribe_monitor = FileMonitor(
    to_transcribe_folder,
    video_extensions,
    self.on_video_for_transcription  # Транскрипция
)
```

### Перемещение файла

```python
def _move_file_to_output(self, file_path):
    """Move uploaded file to output folder"""
    output_folder = self.config.get("output_folder")
    destination_path = os.path.join(output_folder, filename)
    os.rename(file_path, destination_path)  # Перемещение
```

### Извлечение информации из имени файла

```python
# Формат: "Meeting Title_YYYY-MM-DD_HH-MM-SS.mp4"
parts = filename.split(f"_{timestamp_str}")
meeting_title = parts[0].replace("_", " ")
```

---

## 📊 Сравнение логики

### Старая логика `handle_successful_rename()`
```python
1. Копирование в output_folder
2. Если enable_upload:
   - Загрузка на сервер
3. Удаление из watch_folder
```

### Новая логика `handle_successful_rename()`
```python
1. Отправка уведомления в Telegram
2. Файл остается в watch_folder
```

### Новая логика `on_video_for_transcription()`
```python
1. Извлечение информации из имени файла
2. Загрузка на сервер
3. Перемещение в output_folder
4. Обработка транскрипции
```

---

## 🚀 Миграция с старой архитектуры

Если вы используете старую версию:

1. **Обновите конфигурацию:**
   ```json
   {
       "to_transcribe_folder": "D:/Path/To/ToTranscribe",
       "output_folder": "D:/Path/To/Output"
   }
   ```

2. **Создайте папки:**
   ```
   D:\Nextcloud\Videos\ToTranscribe\
   D:\Nextcloud\Videos\Transcribed\
   ```

3. **Перезапустите приложение:**
   ```bash
   python main.py
   ```

4. **Обновите рабочий процесс:**
   - Вместо автоматической загрузки, вручную переносите файлы в `to_transcribe_folder`

---

## 📚 Документация

Новые документы:
- `NEW_ARCHITECTURE.md` - подробное описание новой архитектуры
- `SETUP_NEW_FOLDERS.md` - инструкция по настройке
- `CHANGES_SUMMARY.md` - этот файл

---

## ✅ Проверочный список

После обновления убедитесь, что:

- [ ] Обновлена конфигурация (`config.json`)
- [ ] Созданы папки (`to_transcribe_folder`, `output_folder`)
- [ ] Приложение запускается без ошибок
- [ ] Оба монитора работают (логи содержат `[WATCH_FOLDER]` и `[TO_TRANSCRIBE_FOLDER]`)
- [ ] Переименование работает в `watch_folder`
- [ ] Транскрипция работает в `to_transcribe_folder`
- [ ] Файлы перемещаются в `output_folder` после загрузки

---

## 🎉 Готово!

Приложение теперь работает в двухэтапном режиме с полным контролем пользователя!
