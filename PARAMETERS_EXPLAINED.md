# 🔧 Объяснение основных параметров конфигурации

## 🎯 Два главных параметра

### 1️⃣ `enable_upload` - Включить/отключить транскрипцию

```json
"enable_upload": true
```

**Что это значит:**
- `true` - видео загружаются на сервер транскрипции (Scriberr)
- `false` - видео только переименовываются, транскрипция отключена

**Когда использовать:**
- `true` - если у вас есть сервер транскрипции и вы хотите обрабатывать видео
- `false` - если вам нужно только переименование видео по встречам

**Пример:**
```json
{
    "enable_upload": true,
    "api_base_url": "http://localhost:8080",
    "api_key": "YOUR_API_KEY"
}
```

**Что требуется:**
- `api_base_url` - адрес сервера транскрипции
- `api_key` - ключ доступа к серверу

---

### 2️⃣ `enable_speaker_identification` - Включить/отключить AI определение спикеров

```json
"enable_speaker_identification": true
```

**Что это значит:**
- `true` - приложение использует AI (OpenRouter) для автоматического определения имен спикеров
- `false` - спикеры не определяются автоматически, только ручное редактирование

**Когда использовать:**
- `true` - если у вас есть API ключ OpenRouter и вы хотите автоматическое определение
- `false` - если вы хотите только ручное редактирование спикеров в Telegram

**Пример:**
```json
{
    "enable_speaker_identification": true,
    "openrouter_api_key": "sk-or-v1-YOUR_KEY",
    "openrouter_model": "google/gemini-2.0-flash-001"
}
```

**Что требуется:**
- `openrouter_api_key` - ключ доступа к OpenRouter
- `openrouter_model` - модель AI для использования

---

## 📊 Матрица конфигураций

### Сценарий 1: Только переименование

```json
{
    "enable_upload": false,
    "enable_speaker_identification": false
}
```

**Что происходит:**
1. Видео появляется в `watch_folder`
2. Приложение переименовывает его по названию встречи
3. Файл остается в `watch_folder`
4. Конец

**Требуется:**
- Google Calendar API (для поиска встреч)

---

### Сценарий 2: Переименование + Транскрипция (без AI)

```json
{
    "enable_upload": true,
    "enable_speaker_identification": false,
    "api_base_url": "http://localhost:8080",
    "api_key": "YOUR_API_KEY"
}
```

**Что происходит:**
1. Видео появляется в `watch_folder`
2. Приложение переименовывает его
3. Пользователь переносит в `to_transcribe_folder`
4. Приложение загружает на сервер
5. Ждет завершения транскрипции
6. Предлагает пользователю вручную редактировать спикеров в Telegram
7. Публикует результаты

**Требуется:**
- Google Calendar API
- Сервер транскрипции (Scriberr)

---

### Сценарий 3: Переименование + Транскрипция + AI

```json
{
    "enable_upload": true,
    "enable_speaker_identification": true,
    "api_base_url": "http://localhost:8080",
    "api_key": "YOUR_API_KEY",
    "openrouter_api_key": "sk-or-v1-YOUR_KEY",
    "openrouter_model": "google/gemini-2.0-flash-001"
}
```

**Что происходит:**
1. Видео появляется в `watch_folder`
2. Приложение переименовывает его
3. Пользователь переносит в `to_transcribe_folder`
4. Приложение загружает на сервер
5. Ждет завершения транскрипции
6. **AI автоматически определяет имена спикеров**
7. Предлагает пользователю проверить/отредактировать спикеров в Telegram
8. Публикует результаты

**Требуется:**
- Google Calendar API
- Сервер транскрипции (Scriberr)
- OpenRouter API ключ

---

## 🎯 Выбор конфигурации

### Я хочу только переименование видео

```json
{
    "watch_folder": "D:/Videos/JustRecorded",
    "to_transcribe_folder": "D:/Videos/ToTranscribe",
    "output_folder": "D:/Videos/Transcribed",
    "enable_upload": false,
    "enable_speaker_identification": false,
    "google_credentials_path": "credentials.json",
    "google_token_path": "token.json"
}
```

**Требуется:**
- ✅ Google Calendar API

---

### Я хочу переименование + транскрипцию (без AI)

```json
{
    "watch_folder": "D:/Videos/JustRecorded",
    "to_transcribe_folder": "D:/Videos/ToTranscribe",
    "output_folder": "D:/Videos/Transcribed",
    "enable_upload": true,
    "enable_speaker_identification": false,
    "google_credentials_path": "credentials.json",
    "google_token_path": "token.json",
    "api_base_url": "http://localhost:8080",
    "api_key": "YOUR_API_KEY"
}
```

**Требуется:**
- ✅ Google Calendar API
- ✅ Сервер транскрипции (Scriberr)

---

### Я хочу полный функционал (переименование + транскрипция + AI)

```json
{
    "watch_folder": "D:/Videos/JustRecorded",
    "to_transcribe_folder": "D:/Videos/ToTranscribe",
    "output_folder": "D:/Videos/Transcribed",
    "enable_upload": true,
    "enable_speaker_identification": true,
    "google_credentials_path": "credentials.json",
    "google_token_path": "token.json",
    "google_sheets_id": "YOUR_SHEETS_ID",
    "drive_transcript_folder_id": "YOUR_FOLDER_ID",
    "api_base_url": "http://localhost:8080",
    "api_key": "YOUR_API_KEY",
    "openrouter_api_key": "sk-or-v1-YOUR_KEY",
    "openrouter_model": "google/gemini-2.0-flash-001",
    "telegram_bot_token": "YOUR_BOT_TOKEN",
    "telegram_chat_id": "YOUR_CHAT_ID"
}
```

**Требуется:**
- ✅ Google Calendar API
- ✅ Google Sheets API
- ✅ Google Drive API
- ✅ Сервер транскрипции (Scriberr)
- ✅ OpenRouter API ключ
- ✅ Telegram бот

---

## 📝 Таблица зависимостей

| Параметр | Зависит от | Требует |
|----------|-----------|---------|
| `enable_upload` | - | `api_base_url`, `api_key` |
| `enable_speaker_identification` | `enable_upload` | `openrouter_api_key`, `openrouter_model` |
| `google_sheets_id` | - | Google Sheets API |
| `drive_transcript_folder_id` | - | Google Drive API |
| `telegram_bot_token` | - | Telegram бот |

---

## ❓ Часто задаваемые вопросы

### Q: Что если я установлю `enable_upload: false`?
**A:** Приложение будет только переименовывать видео. Транскрипция не будет работать.

### Q: Что если я установлю `enable_speaker_identification: false`?
**A:** Спикеры не будут определяться автоматически. Вы сможете редактировать их вручную в Telegram.

### Q: Нужен ли мне OpenRouter API ключ?
**A:** Только если вы хотите `enable_speaker_identification: true`.

### Q: Что если я не установлю `api_key`?
**A:** Загрузка видео не будет работать. Установите `enable_upload: false`.

### Q: Можно ли использовать приложение без Google API?
**A:** Да, если установить `enable_upload: false` и не использовать Google Sheets.

### Q: Что если я не установлю Telegram токен?
**A:** Приложение будет работать, но уведомления не будут отправляться.

---

## 🚀 Быстрая настройка

### Шаг 1: Выберите сценарий

- [ ] Только переименование
- [ ] Переименование + транскрипция (без AI)
- [ ] Полный функционал (с AI)

### Шаг 2: Скопируйте конфигурацию

Выберите нужную конфигурацию из раздела "Выбор конфигурации" выше.

### Шаг 3: Добавьте свои значения

Замените `YOUR_API_KEY`, `YOUR_SHEETS_ID` и т.д. на реальные значения.

### Шаг 4: Сохраните в config.json

```bash
cp config.json.example config.json
# Отредактируйте config.json
```

### Шаг 5: Запустите приложение

```bash
python main.py
```

---

## 📚 Дополнительная информация

Для подробной информации см.:
- `CONFIG_REFERENCE.md` - полный справочник всех параметров
- `README.md` - основная информация
- `SETUP.md` - инструкция по установке
