# 🔊 Параметр enable_speaker_identification

## 🎯 Что это?

Параметр `enable_speaker_identification` контролирует, будет ли приложение:
- Пытаться определить спикеров в видео
- Предлагать ручное назначение спикеров в Telegram

## ⚙️ Конфигурация

### config.json

```json
{
    "enable_speaker_identification": true
}
```

### Значения

| Значение | Поведение |
|----------|-----------|
| `true` | Определяет спикеров, предлагает ручное назначение |
| `false` | Не определяет спикеров, не предлагает ручное назначение |

## 🔄 Поведение

### Когда enable_speaker_identification: true

```
1. Видео загружается на сервер
2. Сервер обрабатывает видео
3. ✅ Приложение пытается определить спикеров
4. ✅ Если спикеры не определены:
   └─ Отправляет сообщение в Telegram
   └─ Предлагает ручное назначение
5. ✅ Если спикеры определены:
   └─ Обновляет спикеров на сервере
   └─ Предлагает ручное редактирование
```

### Когда enable_speaker_identification: false

```
1. Видео загружается на сервер
2. Сервер обрабатывает видео
3. ❌ Приложение НЕ пытается определить спикеров
4. ❌ Не отправляет сообщение в Telegram о спикерах
5. ❌ Не предлагает ручное назначение спикеров
6. ✅ Транскрипт сохраняется без имен спикеров
```

## 📊 Сравнение

| Функция | enable_speaker_identification: true | enable_speaker_identification: false |
|---------|-------------------------------------|--------------------------------------|
| Определение спикеров | ✅ Да | ❌ Нет |
| Сообщение в Telegram | ✅ Да | ❌ Нет |
| Ручное назначение | ✅ Да | ❌ Нет |
| Сохранение транскрипта | ✅ С именами | ✅ Без имен |
| Публикация в Sheets | ✅ С именами | ✅ Без имен |

## 🚀 Использование

### Включить определение спикеров

```json
{
    "enable_speaker_identification": true,
    "openrouter_api_key": "sk-or-v1-...",
    "openrouter_model": "google/gemini-2.5-flash-lite"
}
```

**Требуется:**
- OpenRouter API ключ
- Интернет соединение
- Дополнительное время обработки

### Отключить определение спикеров

```json
{
    "enable_speaker_identification": false
}
```

**Преимущества:**
- Быстрее (нет вызовов AI)
- Дешевле (нет API вызовов)
- Проще (нет ручного назначения)

## 💡 Когда использовать

### Включить (true)

- ✅ Нужны имена спикеров в транскрипте
- ✅ Хотите редактировать спикеров вручную
- ✅ Есть OpenRouter API ключ
- ✅ Готовы ждать дополнительное время

### Отключить (false)

- ✅ Имена спикеров не важны
- ✅ Хотите быстрее обрабатывать видео
- ✅ Нет OpenRouter API ключа
- ✅ Хотите сэкономить на API вызовах

## 📝 Примеры

### Пример 1: Включено

```json
{
    "enable_speaker_identification": true,
    "openrouter_api_key": "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",
    "openrouter_model": "google/gemini-2.5-flash-lite"
}
```

**Результат:**
```
✅ Transcription completed: Meeting_2026-01-27_14-30-00.mp4
🔊 Speakers identified: Speaker 1, Speaker 2, Speaker 3
📝 Transcript saved with speaker names
```

### Пример 2: Отключено

```json
{
    "enable_speaker_identification": false
}
```

**Результат:**
```
✅ Transcription completed: Meeting_2026-01-27_14-30-00.mp4
📝 Transcript saved without speaker names
```

## 🔧 Технические детали

### Код

```python
enable_speaker_identification = self.config.get("enable_speaker_identification", True)

if enable_speaker_identification:
    # Определяет спикеров
    identified_speakers = self._identify_speakers(transcript_data, job_id)
    
    # Предлагает ручное назначение
    self._offer_manual_speaker_assignment(job_id, speakers, transcript_data)
else:
    # Пропускает определение спикеров
    logger.info(f"Speaker identification disabled for job {job_id}")
```

### Логирование

**Когда включено:**
```
INFO: Starting speaker identification for job 12345
INFO: Speakers identified: Speaker 1, Speaker 2
```

**Когда отключено:**
```
INFO: Speaker identification disabled for job 12345
```

## ❓ Часто задаваемые вопросы

**Q: Что произойдет, если я отключу speaker identification?**  
A: Спикеры не будут определены, и в Telegram не будет сообщений о спикерах.

**Q: Могу ли я включить/отключить это во время работы?**  
A: Да, просто обновите config.json и перезагрузите приложение.

**Q: Влияет ли это на уже обработанные видео?**  
A: Нет, только на новые видео.

**Q: Что если я включу, но нет API ключа?**  
A: Приложение попытается определить спикеров, но может ошибиться.

**Q: Как это влияет на производительность?**  
A: Включение замедляет обработку на 10-30 секунд (вызов AI).

## 📚 Связанная документация

- `PARAMETERS_EXPLAINED.md` - Объяснение всех параметров
- `CONFIG_REFERENCE.md` - Справочник по конфигурации
- `WHAT_IS_ENABLE_UPLOAD.md` - Объяснение enable_upload

---

**Дата:** 27 января 2026  
**Статус:** ✅ Обновлено  
**Версия:** 2.1
