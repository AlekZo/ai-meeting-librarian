# 🎯 Уточнение: `enable_upload` и двухэтапная архитектура

## ❌ НЕПРАВИЛЬНОЕ ПОНИМАНИЕ

Вы подумали:
- `enable_upload: true` → сразу загружается, не нужно переносить
- `enable_upload: false` → нужно вручную переносить

**Это НЕПРАВИЛЬНО!** ❌

---

## ✅ ПРАВИЛЬНОЕ ПОНИМАНИЕ

### `enable_upload` контролирует **ЧТО ПРОИСХОДИТ** с видео

- `enable_upload: true` → видео **загружается на сервер** для транскрипции
- `enable_upload: false` → видео **НЕ загружается**, только переименование

### Двухэтапная архитектура контролирует **КОГДА** это происходит

- **Этап 1 (watch_folder):** Переименование (всегда автоматическое)
- **Этап 2 (to_transcribe_folder):** Загрузка (только если пользователь переносит файл)

---

## 📊 МАТРИЦА: Как это работает вместе

```
┌─────────────────────────────────────────────────────────────────────────┐
│ ЭТАП 1: watch_folder (ВСЕГДА ОДИНАКОВО)                               │
├─────────────────────────────────────────────────────────────────────────┤
│ 1. Видео появляется в watch_folder                                     │
│ 2. Приложение ВСЕГДА переименовывает его                               │
│ 3. Файл остается в watch_folder                                        │
│                                                                         │
│ Это происходит НЕЗАВИСИМО от enable_upload!                            │
└─────────────────────────────────────────────────────────────────────────┘
                                    ↓
                    (Пользователь решает дальше)
                                    ↓
┌─────────────────────────────────────────────────────────────────────────┐
│ ЭТАП 2: to_transcribe_folder (ЗАВИСИТ от enable_upload)               │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│ Если enable_upload: true                                              │
│ ├─ Пользователь переносит файл в to_transcribe_folder                 │
│ ├─ Приложение ЗАГРУЖАЕТ видео на сервер                               │
│ ├─ Ждет завершения транскрипции                                       │
│ └─ Обрабатывает результаты                                            │
│                                                                         │
│ Если enable_upload: false                                             │
│ ├─ Пользователь может переносить файл, но ничего не произойдет        │
│ ├─ Приложение НЕ загружает видео                                      │
│ └─ Файл просто остается в to_transcribe_folder                        │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 🔄 ЧЕТЫРЕ СЦЕНАРИЯ

### Сценарий 1: `enable_upload: true` (с загрузкой)

```
watch_folder/
└─ Video_2026-01-26_20-30-03.mp4
   ↓ (приложение переименовывает)
   ↓
watch_folder/
└─ Front - Aorta weekly sync_2026-01-26_20-30-03.mp4
   ↓ (пользователь переносит)
   ↓
to_transcribe_folder/
└─ Front - Aorta weekly sync_2026-01-26_20-30-03.mp4
   ↓ (приложение ЗАГРУЖАЕТ на сервер)
   ↓
output_folder/
└─ Front - Aorta weekly sync_2026-01-26_20-30-03.mp4
   ↓ (приложение обрабатывает транскрипцию)
   ↓
Google Sheets + Google Drive
```

**Требуется:**
- ✅ Пользователь переносит файл в `to_transcribe_folder`
- ✅ Сервер транскрипции (Scriberr)
- ✅ API ключ

---

### Сценарий 2: `enable_upload: false` (без загрузки)

```
watch_folder/
└─ Video_2026-01-26_20-30-03.mp4
   ↓ (приложение переименовывает)
   ↓
watch_folder/
└─ Front - Aorta weekly sync_2026-01-26_20-30-03.mp4
   ↓ (пользователь может переносить, но ничего не произойдет)
   ↓
to_transcribe_folder/
└─ Front - Aorta weekly sync_2026-01-26_20-30-03.mp4
   ↓ (приложение НЕ загружает)
   ↓
Файл просто остается в to_transcribe_folder
```

**Требуется:**
- ✅ Пользователь переносит файл (опционально)
- ❌ Сервер транскрипции НЕ требуется
- ❌ API ключ НЕ требуется

---

## 🎯 ОТВЕТ НА ВАШИ ВОПРОСЫ

### Вопрос 1: Если `enable_upload: true`, то не нужно переносить в `to_transcribe_folder`?

**Ответ: НЕПРАВИЛЬНО!** ❌

**Правильно:** Вам **ВСЕГДА** нужно переносить в `to_transcribe_folder`, независимо от `enable_upload`.

- `enable_upload: true` → Приложение **загружает** файл после переноса
- `enable_upload: false` → Приложение **НЕ загружает** файл после переноса

---

### Вопрос 2: Сразу будет запущена транскрипция?

**Ответ: НЕТ!** ❌

**Правильно:** Транскрипция запускается только если:
1. ✅ `enable_upload: true`
2. ✅ Вы переносите файл в `to_transcribe_folder`
3. ✅ Приложение обнаруживает файл в `to_transcribe_folder`

Это **двухэтапный процесс**:
- Этап 1: Переименование (автоматическое)
- Этап 2: Загрузка (ручной перенос файла)

---

### Вопрос 3: Если `enable_upload: false`, то нужно вручную переносить?

**Ответ: ТЕХНИЧЕСКИ ДА, но это бесполезно!** ⚠️

**Правильно:** Вы можете переносить файл, но приложение его не загружает.

Если `enable_upload: false`, то:
- Переносить файл **можно**, но **не нужно**
- Приложение **не загружает** видео
- Файл просто остается в `to_transcribe_folder`

---

## 📋 ТАБЛИЦА ИСТИНЫ

| Ситуация | `enable_upload` | Переносить в `to_transcribe_folder`? | Что происходит? |
|----------|-----------------|--------------------------------------|-----------------|
| Хочу только переименование | `false` | ❌ Не нужно | Файл переименовывается, остается в `watch_folder` |
| Хочу транскрипцию | `true` | ✅ ОБЯЗАТЕЛЬНО | Файл переименовывается, загружается, обрабатывается |
| Забыл переносить | `true` | ❌ Не перенес | Файл переименовывается, остается в `watch_folder`, ничего не загружается |
| Перенес, но `enable_upload: false` | `false` | ✅ Перенес | Файл переименовывается, остается в `to_transcribe_folder`, не загружается |

---

## 🎯 ПРАВИЛЬНАЯ ЛОГИКА

### Если вы хотите транскрипцию:

```
1. Установите enable_upload: true
2. Убедитесь, что есть api_base_url и api_key
3. Видео появляется в watch_folder
4. Приложение переименовывает его
5. ВЫ ПЕРЕНОСИТЕ в to_transcribe_folder (ОБЯЗАТЕЛЬНО!)
6. Приложение загружает на сервер
7. Приложение обрабатывает транскрипцию
```

### Если вы хотите только переименование:

```
1. Установите enable_upload: false
2. Видео появляется в watch_folder
3. Приложение переименовывает его
4. Файл остается в watch_folder
5. Конец (не переносите в to_transcribe_folder)
```

---

## 💡 АНАЛОГИЯ

Представьте, что это **почтовая служба**:

- **watch_folder** = почтовый ящик на улице
- **to_transcribe_folder** = почтовое отделение
- **enable_upload** = есть ли почтовый грузовик

**Сценарий 1: `enable_upload: true`**
```
1. Вы кладете письмо в почтовый ящик (watch_folder)
2. Почтальон забирает письмо и переписывает адрес (переименование)
3. Вы берете письмо и несете в почтовое отделение (to_transcribe_folder)
4. Грузовик забирает письмо и везет на сортировку (загрузка)
5. Письмо обрабатывается и доставляется (транскрипция)
```

**Сценарий 2: `enable_upload: false`**
```
1. Вы кладете письмо в почтовый ящик (watch_folder)
2. Почтальон забирает письмо и переписывает адрес (переименование)
3. Вы берете письмо и несете в почтовое отделение (to_transcribe_folder)
4. Грузовика нет! Письмо просто лежит в отделении (не загружается)
5. Ничего не происходит
```

---

## ✅ ПРАВИЛЬНЫЕ КОНФИГУРАЦИИ

### Конфигурация 1: Только переименование

```json
{
    "enable_upload": false,
    "enable_speaker_identification": false
}
```

**Что делать:**
1. Видео появляется в `watch_folder`
2. Приложение переименовывает его
3. **НЕ переносите в `to_transcribe_folder`**
4. Файл остается в `watch_folder`

---

### Конфигурация 2: Переименование + Транскрипция

```json
{
    "enable_upload": true,
    "enable_speaker_identification": true,
    "api_base_url": "http://localhost:8080",
    "api_key": "YOUR_API_KEY"
}
```

**Что делать:**
1. Видео появляется в `watch_folder`
2. Приложение переименовывает его
3. **ПЕРЕНОСИТЕ в `to_transcribe_folder`** (ОБЯЗАТЕЛЬНО!)
4. Приложение загружает и обрабатывает

---

## ❓ ИТОГОВЫЕ ОТВЕТЫ

### Вопрос: Если `enable_upload: true`, то не нужно переносить?

**Ответ: НЕПРАВИЛЬНО!** Вам **ВСЕГДА** нужно переносить в `to_transcribe_folder`, если вы хотите загрузку.

---

### Вопрос: Сразу будет запущена транскрипция?

**Ответ: НЕТ!** Транскрипция запускается только после переноса файла в `to_transcribe_folder`.

---

### Вопрос: Если `enable_upload: false`, то нужно вручную переносить?

**Ответ: НЕТ!** Если `enable_upload: false`, то переносить **не нужно**. Приложение не загружает видео в любом случае.

---

## 🚀 ИТОГОВАЯ ЛОГИКА

```
enable_upload: true
├─ Переименование: ✅ ВСЕГДА
├─ Перенос в to_transcribe_folder: ✅ ОБЯЗАТЕЛЬНО
└─ Загрузка на сервер: ✅ ЕСЛИ перенесли

enable_upload: false
├─ Переименование: ✅ ВСЕГДА
├─ Перенос в to_transcribe_folder: ❌ НЕ НУЖНО
└─ Загрузка на сервер: ❌ НИКОГДА
```

---

## 📚 Дополнительная информация

Для подробной информации см.:
- `WHAT_IS_ENABLE_UPLOAD.md` - объяснение `enable_upload`
- `PARAMETERS_EXPLAINED.md` - объяснение всех параметров
- `NEW_ARCHITECTURE.md` - архитектура приложения
