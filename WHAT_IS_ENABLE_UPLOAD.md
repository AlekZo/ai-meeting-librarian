# 🎯 Что такое `enable_upload`?

## 📌 Краткий ответ

`enable_upload` - это параметр, который **включает или отключает загрузку видео на сервер транскрипции**.

```json
"enable_upload": true   // Видео загружаются на сервер
"enable_upload": false  // Видео только переименовываются
```

---

## 🔄 Как это работает

### Когда `enable_upload: true`

```
1. Видео появляется в watch_folder
   ↓
2. Приложение переименовывает его
   ↓
3. Пользователь переносит в to_transcribe_folder
   ↓
4. Приложение ЗАГРУЖАЕТ видео на сервер транскрипции
   ↓
5. Ждет завершения транскрипции
   ↓
6. Обрабатывает результаты (спикеры, summary и т.д.)
   ↓
7. Публикует в Google Sheets и Google Drive
```

### Когда `enable_upload: false`

```
1. Видео появляется в watch_folder
   ↓
2. Приложение переименовывает его
   ↓
3. Файл остается в watch_folder
   ↓
4. Конец (транскрипция не происходит)
```

---

## 📊 Сравнение

| Параметр | `true` | `false` |
|----------|--------|--------|
| Переименование | ✅ | ✅ |
| Загрузка на сервер | ✅ | ❌ |
| Транскрипция | ✅ | ❌ |
| Определение спикеров | ✅ | ❌ |
| Публикация в Sheets | ✅ | ❌ |
| Требует API ключ | ✅ | ❌ |

---

## 🎯 Когда использовать

### `enable_upload: true`

**Используйте, если:**
- ✅ У вас есть сервер транскрипции (Scriberr)
- ✅ У вас есть API ключ сервера
- ✅ Вы хотите обрабатывать видео автоматически
- ✅ Вы хотите получать транскрипты и summary

**Требуется:**
- `api_base_url` - адрес сервера
- `api_key` - ключ доступа

**Пример конфигурации:**
```json
{
    "enable_upload": true,
    "api_base_url": "http://localhost:8080",
    "api_key": "sk_test_1a2b3c4d5e6f7g8h9i0j"
}
```

---

### `enable_upload: false`

**Используйте, если:**
- ✅ Вам нужно только переименование видео
- ✅ У вас нет сервера транскрипции
- ✅ Вы не хотите обрабатывать видео автоматически
- ✅ Вы хотите сэкономить на API запросах

**Требуется:**
- Только Google Calendar API

**Пример конфигурации:**
```json
{
    "enable_upload": false,
    "google_credentials_path": "credentials.json",
    "google_token_path": "token.json"
}
```

---

## 📝 Примеры использования

### Пример 1: Только переименование

```json
{
    "watch_folder": "D:/Videos/JustRecorded",
    "to_transcribe_folder": "D:/Videos/ToTranscribe",
    "output_folder": "D:/Videos/Transcribed",
    "enable_upload": false,
    "enable_speaker_identification": false,
    "google_credentials_path": "credentials.json",
    "google_token_path": "token.json"
}
```

**Результат:**
- Видео переименовываются по названиям встреч
- Файлы остаются в `watch_folder`
- Никакой обработки не происходит

---

### Пример 2: Переименование + Транскрипция

```json
{
    "watch_folder": "D:/Videos/JustRecorded",
    "to_transcribe_folder": "D:/Videos/ToTranscribe",
    "output_folder": "D:/Videos/Transcribed",
    "enable_upload": true,
    "enable_speaker_identification": false,
    "google_credentials_path": "credentials.json",
    "google_token_path": "token.json",
    "api_base_url": "http://localhost:8080",
    "api_key": "sk_test_1a2b3c4d5e6f7g8h9i0j"
}
```

**Результат:**
- Видео переименовываются
- Пользователь переносит в `to_transcribe_folder`
- Приложение загружает на сервер
- Получает транскрипт
- Предлагает редактировать спикеров вручную
- Публикует результаты

---

### Пример 3: Полный функционал (с AI)

```json
{
    "watch_folder": "D:/Videos/JustRecorded",
    "to_transcribe_folder": "D:/Videos/ToTranscribe",
    "output_folder": "D:/Videos/Transcribed",
    "enable_upload": true,
    "enable_speaker_identification": true,
    "google_credentials_path": "credentials.json",
    "google_token_path": "token.json",
    "google_sheets_id": "1a2b3c4d5e6f7g8h9i0j",
    "drive_transcript_folder_id": "1a2b3c4d5e6f7g8h9i0j",
    "api_base_url": "http://localhost:8080",
    "api_key": "sk_test_1a2b3c4d5e6f7g8h9i0j",
    "openrouter_api_key": "sk-or-v1-1a2b3c4d5e6f7g8h9i0j",
    "openrouter_model": "google/gemini-2.0-flash-001",
    "telegram_bot_token": "123456789:ABCdefGHIjklmnoPQRstuvWXYZ",
    "telegram_chat_id": "123456789"
}
```

**Результат:**
- Видео переименовываются
- Пользователь переносит в `to_transcribe_folder`
- Приложение загружает на сервер
- Получает транскрипт
- **AI автоматически определяет спикеров**
- Предлагает проверить/отредактировать спикеров
- Публикует результаты в Google Sheets и Drive
- Отправляет уведомления в Telegram

---

## ❓ Часто задаваемые вопросы

### Q: Что если я установлю `enable_upload: false`, а потом захочу включить?

**A:** Просто измените параметр на `true` и перезапустите приложение:

```json
"enable_upload": true
```

Убедитесь, что у вас есть `api_base_url` и `api_key`.

---

### Q: Что если я установлю `enable_upload: true`, но не установлю `api_key`?

**A:** Приложение выведет ошибку при запуске:

```
ValueError: API key is required when upload is enabled
```

Установите `api_key` или измените `enable_upload: false`.

---

### Q: Можно ли изменить `enable_upload` во время работы приложения?

**A:** Нет, нужно перезапустить приложение:

```bash
# Остановите приложение (Ctrl+C)
# Измените config.json
# Запустите снова
python main.py
```

---

### Q: Что если сервер транскрипции недоступен?

**A:** Приложение выведет ошибку:

```
Error: Failed to connect to transcription server
```

Проверьте:
- Запущен ли сервер?
- Правильный ли `api_base_url`?
- Правильный ли `api_key`?

---

### Q: Сколько стоит использование `enable_upload: true`?

**A:** Это зависит от вашего сервера транскрипции:
- Если это локальный сервер (Scriberr) - бесплатно
- Если это облачный сервис - зависит от тарифа

---

## 🚀 Быстрая настройка

### Шаг 1: Решите, нужна ли вам транскрипция

- [ ] Только переименование → `enable_upload: false`
- [ ] С транскрипцией → `enable_upload: true`

### Шаг 2: Если выбрали `enable_upload: true`

Убедитесь, что у вас есть:
- [ ] Сервер транскрипции (Scriberr)
- [ ] `api_base_url` (адрес сервера)
- [ ] `api_key` (ключ доступа)

### Шаг 3: Обновите config.json

```json
{
    "enable_upload": true,
    "api_base_url": "http://localhost:8080",
    "api_key": "YOUR_API_KEY"
}
```

### Шаг 4: Запустите приложение

```bash
python main.py
```

---

## 📚 Дополнительная информация

Для подробной информации см.:
- **CONFIG_REFERENCE.md** - полный справочник всех параметров
- **PARAMETERS_EXPLAINED.md** - объяснение параметров с примерами
- **PARAMETERS_SUMMARY.txt** - краткое резюме
- **README.md** - основная информация
