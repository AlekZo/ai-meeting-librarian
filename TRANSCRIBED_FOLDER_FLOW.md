# 📁 Поток обработки файлов: ToTranscribe → Transcribed

## 🎯 Обзор

После окончания транскрибции файл **автоматически перемещается** из папки `ToTranscribe` в папку `Transcribed`.

```
watch_folder (JustRecorded)
    ↓
    [Переименование по названию встречи]
    ↓
    Файл остается в watch_folder
    ↓
    [Пользователь переносит в ToTranscribe]
    ↓
to_transcribe_folder (ToTranscribe)
    ↓
    [Загрузка на сервер]
    ↓
    [Транскрибция]
    ↓
    [Обработка результатов]
    ↓
    ✅ ФАЙЛ ПЕРЕМЕЩАЕТСЯ В Transcribed
    ↓
transcribed_folder (Transcribed)
    └─ Архив обработанных видео
```

## 📋 Конфигурация

### config.json

```json
{
    "watch_folder": "D:/Nextcloud/Videos/ScreenRecordings/JustRecorded",
    "to_transcribe_folder": "D:/Nextcloud/Videos/ScreenRecordings/ToTranscribe",
    "transcribed_folder": "D:/Nextcloud/Videos/ScreenRecordings/Transcribed",
    "enable_upload": true
}
```

### Параметры

| Параметр | Описание | Пример |
|----------|---------|--------|
| `watch_folder` | Папка для новых видео | `D:/Videos/JustRecorded` |
| `to_transcribe_folder` | Папка для видео, готовых к транскрибции | `D:/Videos/ToTranscribe` |
| `transcribed_folder` | Папка для обработанных видео | `D:/Videos/Transcribed` |
| `enable_upload` | Включить загрузку на сервер | `true` / `false` |

## 🔄 Процесс обработки

### Этап 1: Переименование (watch_folder)

```
1. Видео появляется в watch_folder
   └─ Video_2026-01-27_14-30-00.mp4

2. Приложение ищет встречу в Google Calendar
   └─ Находит: "Front - Aorta weekly sync"

3. Файл переименовывается
   └─ Front - Aorta weekly sync_2026-01-27_14-30-00.mp4

4. Файл остается в watch_folder
   └─ ⚠️ Пользователь должен переносить!
```

### Этап 2: Транскрибция (to_transcribe_folder)

```
1. Пользователь переносит файл в to_transcribe_folder
   └─ Front - Aorta weekly sync_2026-01-27_14-30-00.mp4

2. Приложение обнаруживает файл
   └─ Событие: on_video_for_transcription()

3. Файл загружается на сервер
   └─ POST /upload → job_id

4. Запускается транскрибция
   └─ Scriberr API обрабатывает видео

5. Результаты обрабатываются
   ├─ Извлекаются спикеры
   ├─ Генерируется summary
   └─ Публикуются в Google Sheets

6. ✅ ФАЙЛ ПЕРЕМЕЩАЕТСЯ В transcribed_folder
   └─ Front - Aorta weekly sync_2026-01-27_14-30-00.mp4
   └─ Удаляется из to_transcribe_folder
```

### Этап 3: Архив (transcribed_folder)

```
transcribed_folder/
├─ Front - Aorta weekly sync_2026-01-27_14-30-00.mp4
├─ Front - Aorta weekly sync_2026-01-27_14-30-00_transcript.txt
├─ Another Meeting_2026-01-27_15-00-00.mp4
├─ Another Meeting_2026-01-27_15-00-00_transcript.txt
└─ ...
```

## 💡 Ключевые моменты

### ✅ Что происходит автоматически

1. **Переименование** - файл переименовывается по названию встречи
2. **Загрузка** - файл загружается на сервер (если `enable_upload: true`)
3. **Транскрибция** - видео обрабатывается сервером
4. **Перемещение** - файл перемещается в `transcribed_folder` ✨ **НОВОЕ**
5. **Публикация** - результаты публикуются в Google Sheets

### ⚠️ Что требует действия пользователя

1. **Перенос файла** - пользователь переносит файл из `watch_folder` в `to_transcribe_folder`
   - Это дает контроль над тем, какие видео обрабатывать
   - Можно отложить обработку на потом

### 🚫 Что НЕ происходит

- Файл **НЕ удаляется** из `to_transcribe_folder` вручную
- Файл **НЕ копируется** в `transcribed_folder` - он **ПЕРЕМЕЩАЕТСЯ**
- Файл **НЕ остается** в `to_transcribe_folder` после обработки

## 📊 Сравнение: До и После

### ❌ Старое поведение

```
watch_folder
    ↓ [Переименование]
    ↓ [Копирование в output_folder]
    ↓ [Загрузка]
    ↓ [Транскрибция]
    ↓ [Удаление из watch_folder]
    ↓
output_folder (архив)
```

**Проблемы:**
- Файл копировался, занимая место
- Нет явного контроля над тем, когда начинается обработка
- Файл удалялся из watch_folder, теряя исходное место

### ✅ Новое поведение

```
watch_folder
    ↓ [Переименование]
    ↓ [Пользователь переносит]
    ↓
to_transcribe_folder
    ↓ [Загрузка]
    ↓ [Транскрибция]
    ↓ [ПЕРЕМЕЩЕНИЕ в transcribed_folder]
    ↓
transcribed_folder (архив)
```

**Преимущества:**
- ✅ Явный контроль: пользователь решает, когда начинать обработку
- ✅ Экономия места: файл перемещается, не копируется
- ✅ Ясный поток: три отдельные папки для трех этапов
- ✅ Безопасность: исходный файл не удаляется, а архивируется

## 🔧 Технические детали

### Метод: `_move_file_to_output()`

```python
def _move_file_to_output(self, file_path):
    """Move uploaded file to transcribed folder"""
    try:
        transcribed_folder = self.config.get("transcribed_folder")
        if not transcribed_folder or not os.path.exists(transcribed_folder):
            logger.warning(f"Transcribed folder not configured or doesn't exist")
            return False
        
        filename = os.path.basename(file_path)
        destination_path = os.path.join(transcribed_folder, filename)
        
        # Check if file still exists
        if not os.path.exists(file_path):
            logger.warning(f"Source file no longer exists: {file_path}")
            return False
        
        # Move file to transcribed folder
        logger.info(f"Moving file to transcribed folder: {destination_path}")
        os.rename(file_path, destination_path)
        logger.info(f"✓ File moved to transcribed folder: {destination_path}")
        return True
```

### Когда вызывается

```python
def _process_upload(self, file_path, file_name):
    """Process video upload"""
    try:
        # ... upload logic ...
        
        if response.status_code == 200:
            job_id = response.json().get("job_id")
            
            # Move file to transcribed folder after successful upload
            self._move_file_to_output(file_path)  # ← ЗДЕСЬ
            
            # Start transcription
            self.start_transcription(job_id, file_path)
```

## ❓ Часто задаваемые вопросы

### Q: Что если файл не переместился?

**A:** Приложение логирует ошибку и продолжает работу. Файл остается в `to_transcribe_folder`.

```
⚠️ WARNING: Transcribed folder not configured or doesn't exist
```

### Q: Могу ли я вручную переместить файл обратно?

**A:** Да, но это не рекомендуется. Если вы переместите файл обратно в `to_transcribe_folder`, приложение попытается загрузить его снова.

### Q: Что если я удалю файл из `transcribed_folder`?

**A:** Это безопасно. Файл уже обработан, и все результаты сохранены в Google Sheets и Google Drive.

### Q: Может ли быть дублирование файлов?

**A:** Нет. Файл **перемещается** (не копируется), поэтому он существует только в одном месте.

### Q: Как долго файл остается в `transcribed_folder`?

**A:** Столько, сколько вы хотите. Это архив обработанных видео. Вы можете удалить старые файлы вручную.

## 🚀 Рекомендации

### Структура папок

```
D:\Nextcloud\Videos\ScreenRecordings\
├─ JustRecorded\          ← watch_folder (новые видео)
│  └─ Video_*.mp4
├─ ToTranscribe\          ← to_transcribe_folder (готовые к обработке)
│  └─ Meeting Name_*.mp4
└─ Transcribed\           ← transcribed_folder (обработанные)
   ├─ Meeting Name_*.mp4
   └─ Meeting Name_*_transcript.txt
```

### Рабочий процесс

1. **Запись** → видео появляется в `JustRecorded`
2. **Переименование** → приложение переименовывает файл
3. **Выбор** → пользователь переносит в `ToTranscribe`
4. **Обработка** → приложение загружает и обрабатывает
5. **Архив** → файл перемещается в `Transcribed`

### Очистка

- Удаляйте старые файлы из `Transcribed` по мере необходимости
- Не удаляйте файлы из `ToTranscribe` вручную (приложение это сделает)
- Не удаляйте файлы из `JustRecorded` вручную (они могут быть переименованы)

## 📚 Связанная документация

- `NEW_ARCHITECTURE.md` - Полная архитектура приложения
- `PARAMETERS_EXPLAINED.md` - Объяснение всех параметров конфига
- `SETUP_NEW_FOLDERS.md` - Инструкции по настройке папок

---

**Дата:** 27 января 2026  
**Статус:** ✅ Реализовано  
**Версия:** 2.0 (с перемещением в transcribed_folder)
